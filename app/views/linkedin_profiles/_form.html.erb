<%= form_for(@linkedin_profile) do |f| %>
  <% if @linkedin_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@linkedin_profile.errors.count, "error") %> prohibited this linkedin_profile from being saved:</h2>

      <ul>
      <% @linkedin_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
